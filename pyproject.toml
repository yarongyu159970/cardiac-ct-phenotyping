[tool.black]
line-length = 120
target-version = ["py39", "py310", "py311"]

[tool.isort]
profile = "black"
line_length = 120
known_first_party = [
    "common", "clustering_features", "candidate_features", "graf_ipcw", "competing_cox", "survival",
    "figures", "oof_summary", "calibration", "hard_mace_bootstrap", "tables", "model_comparison",
]
